/**
 * Rotas que abrem com hero escuro (bg-deep): o header começa transparente com texto claro.
 * Fica aqui para Header e páginas usarem a mesma lista.
 */
export const DARK_HERO_ROUTES = new Set(['/', '/galeria', '/agendamento', '/equipe', '/contato'])
